# v2.2.10 — 문서 기준 버전 초기화

날짜: 2026-04-06

## 개요

5개 레포를 v2.2.10 기준으로 통일하고, 전체 문서를 현재 코드 기준으로 재검토/재작성.

## admin-portal 화면 문서 (43개)

| 대메뉴 | 파일 수 | SA/TA 구분 |
|--------|--------|-----------|
| 포털 | 10 | SA 전용(관리자 UI) + TA 전용(사용자 UI, 콘텐츠 7종) |
| 테넌트 | 1 | SA/TA 공통 |
| 서비스 | 2 | SA 전용(보안 인증서) + TA 전용(접근통제/외부연동/Push) |
| 정책 | 8 | SA/TA 공통 |
| 템플릿 | 1 | SA/TA 공통 |
| 시스템 자원 | 5 | SA/TA 공통 |
| 가상 PC | 5 | TA 전용 |
| 가상 디스크 | 1 | TA 전용 |
| 사용자 정보 | 3 | TA 전용 |
| 사용자 지원 | 3 | TA 전용 |
| 관리자 | 4 | SA/TA 공통(관리자 계정) + SA 전용(시스템 설정, RBAC) + TA 전용(권한 그룹) |

### 미작성 메뉴

- 대시보드 (SA/TA)
- 모니터링 (SA/TA)
- 통계 (SA/TA)

## API 명세서

### admin-portal (28개 파일)

resource 14개 + operation 14개. 백엔드 DTO 기반 전면 검증 완료 (app-ms-resource, app-ms-operation).

fixture ↔ 명세서 ↔ OpenAPI 3-way 정합성 검증 2회 실행.

### user-portal (21개 파일)

resource/operation 5개 DTO 기반 검증 완료. 나머지는 해당 MS 미존재로 스킵.

## OpenAPI (Hoppscotch용)

`_workspace/openapi-admin.yaml` — admin-portal resource + operation 통합.

| 항목 | 수치 |
|------|------|
| Paths | 259 |
| Schemas | 224 |
| Version | v2.2.10 |

tag 기반 폴더 분류 (resource/vpcs, operation/usb 등). Hoppscotch Import from OpenAPI로 사용.

## mock 서버

- fixture 44개 DTO 기반 필드명 교정
- pageinfo.count string → number 14개 수정
- interceptor 호환 응답 구조 정리 (이중 래핑 분석)
- 누락 핸들러 20+ 추가 (usergroups, accounts, device-identifier, port, pooled/user_vm 등)
- codeMap 코드 그룹 4개 추가 (U003, U004, J004, V003)
- U017 VM 할당 상태 2개 → 6개 확장
- NAS handler POST → GET 수정
- storage fixture 필드명 오타 수정 (basic_volm_typ_yn → bas_volm_typ_yn)

## 문서 체계

- PRD → IA → FS → API 4계층 연결 완성
- frontmatter 4타입 (screen/flow/internal/concept) 84개 파일 적용
- IA 9개 문서 + PRD 1개
- .claude/rules/ 규칙 8개 (docs-sa-ta, docs-api, docs-sync, docs-writing, docs-openstack, docs-versioning, docs-release, mock-server)

## 하네스 (AI 에이전트 자동화)

| 하네스 | 에이전트 | 용도 |
|--------|---------|------|
| api-quality | api-reviewer, api-fixer, api-openapi-gen | DTO 대조 / 3-way 정합성 / OpenAPI 변환 |

모드: dto-review, consistency, openapi-gen, full.

## 기타 문서

- OpenStack/VDI 개념: openstack/ (12파일)
- VDI 용어: term/ (3파일)
- 추측 문서: speculation/ (4파일)
- user-portal: vpcinfo, support, auth, mobile, internals, term (40+ 파일)
